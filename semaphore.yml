version: v1.0
name: Hello World CI Pipeline

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004

blocks:
  - name: Install and Run
    task:
      jobs:
        - name: Install Numpy and Run Script
          commands:
            - checkout
            - pip install -r requirements.txt
            - python Hello_World.py
